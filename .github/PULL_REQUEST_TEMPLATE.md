## What changed and why

<!-- One or two sentences. If this changes a variable or output, say so explicitly — it determines whether the next tag is a major, minor, or patch bump. -->

## Checklist

- [ ] `terraform fmt` / `terraform validate` / `tflint` / Trivy pass
- [ ] `terraform test` passes (mocked provider, no AWS credentials needed)
- [ ] README updated if behavior, variables, or outputs changed
- [ ] `CHANGELOG.md` updated
- [ ] Breaking changes to variables/outputs called out above
